# Validol

Validol is an util that validates that object has defined props.

## Arguments

- object `Object`
- props `String || Number || Object || Array`

## Result

```javascript
{
	error: false || Error,
	result: Object,
	all: Bool,
	any: Bool
}
```
